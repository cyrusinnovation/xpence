<!DOCTYPE html>
<html>
<head>
  <title>Xpence</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div>
    <% if current_user %>
      Signed in as <%= current_user.name.titleize %>
      <%= link_to 'Sign Out', signout_path %>
    <% else %>
    <%= link_to 'Sign in w Google Apps', 'auth/google_apps' %>
    <% end %>
  </div>
  <% if current_user %>
  <nav>
    <h2>Navigation</h2>
    <ul>
      <li><%= link_to 'Edit Profile', edit_employee_path(current_user) %></li>
    </ul>
  </nav>
  <% end %>
<%= yield %>

</body>
</html>
